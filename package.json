{
  "name": "hives",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "server": "npm run start --prefix ./server",
    "client": "npm run dev --prefix ./client",
    "build-client": "npm run build --prefix ./client && cp -r ./client/dist/* ./server/static/"
  },
  "author": ""
}
