const dbConfig = {
  dbHost: 'localhost',
  dbName: 'test',
  dbUser: 'test',
  dbPass: 'test',
  // forceSyncModels = true - will drop the Model's table if it already exists
  forceSyncModels: true
};

module.exports = dbConfig;